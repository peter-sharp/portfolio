const gulp = require('gulp');
const site = require('src/site.mjs');

gulp.task('generate', function() {
  // load pages
  // figure out routes
  // figure out templates
  // use app.toString to get html content for each page
  // figure out destinations
  // save html to destinations
})
